{% extends 'base.html.twig' %}

{% block title %}Slides{% endblock %}

{% block body %}
    <h1>Slides</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slide.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ slide.titre }}</td>
            </tr>
            <tr>
                <th>SousTitre</th>
                <td>{{ slide.sousTitre }}</td>
            </tr>
            <tr>
                <th>ImageSlides</th>
                <td>{{ slide.imageSlides }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_slides_index') }}">back to list</a>

    <a href="{{ path('app_slides_edit', {'id': slide.id}) }}">edit</a>

    {{ include('slides/_delete_form.html.twig') }}
{% endblock %}
