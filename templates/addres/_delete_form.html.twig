<form method="post" action="{{ path('app_addres_delete', {'id': addre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ addre.id) }}">
    <button class="btn">Delete</button>
</form>
