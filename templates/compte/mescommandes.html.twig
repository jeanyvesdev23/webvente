{% extends 'base.html.twig' %}

{% block title 'Mes commandes' %}

{% block body %}
	<div class="container mt">
		<div class="compte-user row">
			<div class="col-3">
				{% include "./partial/_compteuser.html.twig" %}
			</div>
			<div class="p-3 br col-9">
				{% if commandes| length > 0 %}

					<div class="fav">
						<h4>Votre commandes</h4>
					</div>
					<div class="table">
						<table class=" mt-4 table">
							<thead>
								<tr>
									<th>#Order</th>
									<th>Date</th>
									<th>Order Qte</th>
									<th>Order total</th>
									<th>Order status</th>
									<th>Actions</th>
								</tr>
							</thead>
							<tbody>
								{% for commande in commandes %}
									<tr>
										<td>{{commande.codeCommande |  date("Hi") }}</td>
										<td>{{commande.createdAt | date("d/m/Y")}}</td>
										<td class="text-center">{{commande.quantite}}</td>
										<td>{{commande.subtotal}}
											Ar</td>
										<td>
											<span class="badge  {{commande.statusCommandes.style|default(" bg-secondary")}}">{{commande.statusCommandes|default("enregistré")}}</span>
										</td>
										<td>
											<a href="{{path("app_compte_commandes_show",{id:commande.id})}}">

												<span type="button" class="btn btn-sm rounded-pill btn-outline-primary ">VOIR COMMANDE</span>
											</a>
										</td>
									</tr>
								{% endfor %}

							</tbody>
						</table>
					</div>
				{% else %}
					<div class="fav">
						<h4>Vous n'êtes pas encore commande
						</h4>
					</div>
				{% endif %}
			</div>
		</div>
	</div>
{% endblock %}
